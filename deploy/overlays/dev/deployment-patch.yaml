apiVersion: apps/v1
kind: Deployment
metadata:
  name: binance-consumer
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: binance-consumer
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        env:
        - name: LOG_LEVEL
          value: "debug"